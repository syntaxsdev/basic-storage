from setuptools import setup, find_packages

setup(
    name='storage',
    version='1.0.0',
    packages=['storage'],
    description='JSON and Key storage with folders',
    author='syntaxs',
    author_email='sidglinton@email.com',
    url='https://github.com/syntaxsdev/basic_storage',
)